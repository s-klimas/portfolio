spring:
  application:
    name: Portfolio
  config:
    import: optional:file:./config.yml
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        model: gpt-5.6-luna
      embedding:
        model: text-embedding-3-small
